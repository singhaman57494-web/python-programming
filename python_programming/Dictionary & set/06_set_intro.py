#                                     set in python

# set is the collection of the unordered items.
# each element in the set must be unique & immutable.

## set is mutable but set value is immutable.

nums = {1 , 2, 3, 4, }
set2 = {2 , 2, 1 , 3 , 2}

print(nums)
print(set2)
# repeated elements stored only once, so it resolved to (1, 2, 3)
print(type(set2))

# empty set

collection = {}

print(collection)
